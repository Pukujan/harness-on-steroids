# v11 — wait clusters

`codex-work-wait-clusters.md`: 222 wait-runs (median 1). 18/31 wait sessions have a run of length ≥2. After a wait-run, next is exec 211 / send 9 / end 2, never apply_patch.

Does not replace v9 insertion counts.
