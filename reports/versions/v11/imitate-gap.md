# Imitate gap (iteration 7 / v11)

v7–v10 stand. This slice: Work **wait clusters**, then look again — never patch.

## Wait clusters

222 wait-runs, median length 1, but **18/31** wait sessions have a cluster ≥2 (73 runs of length 3). After a wait-run: exec **211**, send 9, end 2, patch **0**. After wait_agent: list_agents / exec / send_message, never patch.

Modes: you may wait more than once; after wait, look or Task, do not Edit.

Global Kilo/OpenCode Codex agents re-synced.

## Still open

Kilo R2/R5 (look then edit). OpenCode R6 (todo first). Owner decides match. Do not SWE-bench.
