# Imitate gap (iteration 26 / v30)

Scorer skips `session.agent=plan`. Kilo **7** plan sessions dropped; remaining **13**, work_match still **1/13 (0.08)**. OpenCode skipped 3 plan. No `codex` agent slug in sqlite yet (`code` / `build`).

Owner decides match. Do not SWE-bench.
