# v14 — R4 no-todowrite

Work `update_plan` is 0. Scorer `r4_no_todowrite` fails if the session used todowrite.

Kilo 7/17 pass (10 todowrite sessions). OpenCode 11/24.

`multi_piece` no longer counts todowrite (only task / wait-ish).
