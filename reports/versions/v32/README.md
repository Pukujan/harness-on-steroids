# v32 — mixed-corpus first tool is calls only

exec 1131, shell_command 53, update_plan 9. create_thread dropped. `tool_name()` in gold_analysis now requires custom_tool_call/function_call so a regen does not restore output-as-first.
