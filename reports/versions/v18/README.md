# v18 — fail masks

`fail_mask` names which of R1–R6 a session misses.

Kilo: R2+R4+R5 in 10/17 (write + todowrite after look).
OpenCode: R3 (bash-first) on almost every miss.

Modes already encode the fix. Next live Codex-mode sessions should be re-scored.
