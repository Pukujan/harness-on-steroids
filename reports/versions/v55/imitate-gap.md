# Imitate gap (iteration 51 / v55)

work_match by agent: Kilo `code` **1/13**, `plan` 0/7. OpenCode `build` **3/19**, `plan` 0/3, `luna` 1/1. No `codex` slug. Score code/build as the copies. Owner decides match. Do not SWE-bench.
