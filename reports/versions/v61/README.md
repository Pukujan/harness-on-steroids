# v61 — matched-task /goal prompt + extract script

`spec/matched-task-goal.md` is the copy-paste `/goal`. `research/extract_replay.py` writes gitignored `data/replay/`. Tests do not read real user bodies.
