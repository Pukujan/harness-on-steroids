# Imitate gap (iteration 50 / v54)

OpenCode sqlite (153 sessions) also has **no `codex` slug** (`build` 96, `luna` 48, plan 3, …). Kilo: `code`/`plan` only. `/goal` `agent: codex` does not show up in session.agent. Issue 12 cannot isolate Codex-mode turns by sqlite agent name. Owner decides match. Do not SWE-bench.
