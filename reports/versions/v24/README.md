# v24 — empty Work sessions

4 of 87 Work files have no custom_tool_call/function_call. Line counts 14, 14, 24, 25. Messages and task_started/complete only.

The originator table’s 95 is extra session_meta rows, not 95 distinct tool-call sessions.
