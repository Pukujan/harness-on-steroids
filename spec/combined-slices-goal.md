# Combined slices `/goal` (copy-paste when owner says go)

**Not started.** Ordinary chat is not this file. Paste into Set goal only after owner go. Do not add `.kilo/command/goal.md`.

```
Outcome: Imitate ChatGPT Work in Kilo and OpenCode. Gold stays local transcripts. Do not SWE-bench. Do not treat the chat as the project. Work in modules (spec/repo-modules.md): contract, measure, adapt, lib, gate.

Slice 1 (issue 13): extra OpenCode turns in isolated oc-sandbox, one hash one turn, 180s, --agent codex, --turn. Model openrouter/inclusionai/ling-3.0-flash-fin:free. No live Work cwd. No extra-turn 6eb8631b71ff 0d6ca4607eaf 2bde00530ddd 0aecd1eabdf2 1442d08cf2d3 1b09f49da9b9. score_replay_copies.py. pytest replay tests green.

Slice 2 (issues 14–15): structure-only JSONL counts — assistant-turn shape, send_message briefs, user-turn classes, compacted events. Recode synthesize-late + provenance (path/command/hash or not observed) + research gates into spec and both Codex modes together. Snapshot reports/versions/. No summarizer until compacted report exists.

Slice 3 (issue 16): non-destructive checkpoints/CURRENT.md + one task wrapping the active slice. Do not overwrite AGENTS.md PLAN.md HANDOFF.md. Refresh STATUS/README from evidence. Scratchpad is git, not a compressor.

Slice 4 (issue 17): after slice 2 is in spec, portable pack (spec + mapping + tests + adapters). Pi/Hermes later. Not a new product.

Slice 0 (issue 18): keep src/ lint/types green (ruff, mypy on src/). New code follows spec/repo-modules.md. No scripts at repo root.

Verification: pytest owner-gate including tests/test_work_research_gates.py tests/test_repo_modules.py tests/test_replay_scores.py -q. Never print user.md. Never commit data/ or .env.

Stop when: slice 1 develop notes for extra turns; slice 2 reports exist and modes paired; slice 3 CURRENT exists; slice 4 pack named in spec; issue 18 ruff+mypy on src in CI. Owner can extend.
```
