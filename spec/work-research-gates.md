# Work research / planning / provenance gates

Owner: tool-call histograms are not how Work researches or plans. This spec is the durable plan for those gaps. **Not complete.**

## Gold still

`codex_work_desktop`. Not vscode. Not SWE-bench. Not Harbor. Not a summarizer product.

Work treats **tool output as memory**. It waits on cells, then writes a short brief to a named child. **Do not treat the chat as the project.** `update_plan` is 0. The plan is `send_message` (unscored bodies) or a look-only stop.

## Measure first (counts only, no bodies in git)

- Assistant-turn shape (including heading-like vs plain)
- `send_message` brief structure
- User-turn classes (question / go / correction)
- `compacted` events (listed in corpus, never opened)
- Same-thread `wait(cell_id)` vs wakeup-as-long-run

Do not build compression until `compacted` is a report. Lost in the Middle / RULER / LLMLingua / MemGPT are warnings, not the exam.

## Gates

| Gate | Work-like test |
| --- | --- |
| Research valid | Claim tied to a tool result, or **not observed** |
| Research enough | Target of the next action was actually read; stop if nothing to look up |
| Action reliable | No send/write until that; look/test after write |
| Provenance | Path / command / hash when the answer needs the repo; JOURNAL is not fact |
| Synthesize late | Child brief, user answer, or git checkpoint — not a plan file, not a running novel |

## Recode

After reports exist: `spec/codex-imitate-mode.md` and both Codex modes, paired. Snapshot `reports/versions/`.

## Continuity

Scratchpad = git checkpoint, not in-window summary. PCM-shaped CURRENT/TASK only if they do not overwrite `AGENTS.md` / `PLAN.md` / `HANDOFF.md`.

## Status

Queued as PLAN G–I and ISSUES 14–17. Measurement **not run**.
